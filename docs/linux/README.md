# Linux

[[toc]]

## Linux Commands

### Check os version

```bash
# Find os version
cat /etc/os-release

# Find kernal version
uname -r
```

### Run a command n times

```bash
for i in {1..10}; do <command>; done
```
